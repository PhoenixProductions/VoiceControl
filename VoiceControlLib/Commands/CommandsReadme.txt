Commands

Commands represents a complex spoken interaction with the Voice Control Library.

Each command may provide multiple command triggers (i.e. patterns of speech).

The UserSpeechCommand is special in that it will trigger a sequence of user-specified actions in response to its triggering.